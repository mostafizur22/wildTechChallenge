{% extends 'base.html.twig' %}

{% block title %}Argonaute index{% endblock %}

{% block body %}
    <h1>Argonaute index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for argonaute in argonautes %}
            <tr>
                <td>{{ argonaute.id }}</td>
                <td>{{ argonaute.name }}</td>
                <td>
                    <a href="{{ path('app_argonaute_show', {'id': argonaute.id}) }}">show</a>
                    <a href="{{ path('app_argonaute_edit', {'id': argonaute.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_argonaute_new') }}">Create new</a>
{% endblock %}
